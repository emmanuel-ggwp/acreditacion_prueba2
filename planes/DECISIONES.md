# Decisiones de producto tomadas durante la ejecución nocturna

**Para Emmanuel, a revisar por la mañana.** Cada entrada es una decisión que el plan no traía
cerrada y que se tomó para no detener el trabajo. Ninguna es irreversible sin aviso: la columna
*Coste de cambiarla* dice qué implicaría revertirla.

## Cómo se toman

El criterio, fijado el **2026-08-05**, es **el equilibrio entre terminar cuanto antes y que el
producto quede suficientemente funcional para trabajarlo**. En la práctica:

1. **Se decide, no se pregunta.** Una decisión razonada y registrada vale más que una fase parada
   esperando respuesta.
2. **Gana la opción que desbloquea sin cerrar puertas.** Ante dos alternativas defendibles, la que
   se pueda cambiar después con menos trabajo.
3. **Se prefiere lo que ya afirma el código** a inventar comportamiento nuevo. Si el modelo declara
   un valor por defecto y el esquema declara otro, la decisión suele ser cuál de los dos vale, no
   cuál inventamos.
4. **No se sobre-construye.** Si algo es funcional con la mitad del trabajo, se hace la mitad y se
   registra la otra mitad como deuda.
5. **El crítico evalúa la decisión, no solo el código.** Si la considera equivocada, la corrige y
   deja constancia de ambas posturas — el desacuerdo es información, no ruido.

## Lo que NO se decide sola

Cualquier cosa que sea **destructiva o difícil de revertir**: borrar datos, reescribir la historia
de git, cambiar el modelo de datos de forma incompatible, o alterar lo que un usuario final ya ve
de manera que no se pueda deshacer. Eso se deja propuesto y **se espera**.

---

## Registro

> Cada plan añade sus filas aquí al terminar. Formato obligatorio: qué se decidió, por qué, qué se
> descartó y qué costaría cambiarla. Una decisión sin la alternativa descartada no está registrada,
> está afirmada.
>
> **Esquema de numeración (fijado 2026-08-06, plan 02 fase 2):** los IDs `Dn.m` a secas chocan
> entre planes — ya hay un `D2.1` del plan 01 y las tablas del plan 02 empiezan en `D2.1`. Desde
> ahora las entradas nuevas llevan el plan como prefijo: **`P<plan>-D<n.m>`** (p. ej. `P02-D2.1`
> es la decisión D2.1 de la tabla del plan 02). Las filas ya escritas conservan su ID — la
> columna *Plan · Fase* las desambigua — y no se renumeran, igual que en el backlog.

> **AVISO — revisión de la mañana (plan 02, cerrado 2026-08-06).** Mira primero:
>
> - **D2.3 lleva una corrección del crítico** (marcada en su fila): la frase «quien acierta la
>   contraseña entra aunque la cuota esté gastada» era **falsa en el caso límite** — con la cuota
>   agotada (10/10) el acierto también recibe 429 hasta que expira la ventana de 900 s. El
>   **código está bien** (dejar pasar el acierto con cuota a cero permitiría adivinar
>   indefinidamente); lo que sobreprometía era la documentación. Comentarios del código
>   rectificados en `603a080`; la fila D2.3 de esta tabla, rectificada al cierre.
> - Ninguna decisión del plan 02 quedó «para Emmanuel», pero hay **una propuesta condicionada
>   esperándote** en la sección **«Esperan tu respuesta»**, más abajo.

| # | Plan · Fase | Decisión | Por qué | Alternativa descartada | Coste de cambiarla |
|---|---|---|---|---|---|
| **D0** | **Tomada por Emmanuel** · 2026-08-06 | **Base de datos administrada de DigitalOcean**, no PostgreSQL en el droplet | El servidor anterior se comprometió y hubo que destruirlo **sin snapshot**. Con la base en la misma máquina, los datos mueren con ella. Backups y parches gestionados, y sobrevive a un redespliegue del droplet | **PostgreSQL local con backups propios.** Descartada porque «como la vez anterior» significa que la vez anterior **no había backups**, y montarlos, probar que restauran y vigilarlos es trabajo recurrente que se abandona a los meses | **Alto una vez haya datos reales**: migrar una base en producción. Barato ahora, antes del primer evento |
| **D1.1** | 01 · Fase 1 | **El cupo por defecto pasa de 0 a 2**, en `eventSchemas.ts:97` y en `EventForm.tsx`. `0` sigue siendo válido, pero hay que **escribirlo**: significa «sin invitados» | El `.default(0)` del esquema pisaba el `defaultValue: 2` del modelo, así que **todo evento creado desde la aplicación nacía con cupo 0** y el registro descartaba a todos los acompañantes. Medido: mismo formulario, mismo campo sin tocar → `0` antes, `2` después | **Que `0` signifique «sin configurar» y el handler lo tradujera a 2 al vuelo.** Descartada porque deja dos significados en el mismo valor: un organizador que escriba 0 a propósito no podría expresar «sin invitados», y el dato guardado seguiría mintiendo sobre lo que el evento permite | **Baja.** Es un `.default()` en un sitio y el valor inicial del formulario. Los eventos ya creados conservan su valor: la decisión no reescribe nada |
| **D1.2** | 01 · Fase 1 | **Las cargas precargadas por el organizador NO consumen el cupo del asistente.** Se distinguen con la columna nueva `guests.registration_source` (`MANUAL` / `IMPORT` / `PUBLIC_FORM`); el handler cuenta solo `PUBLIC_FORM` | `guestType` no servía: es una etiqueta libre que el administrador configura por evento. Sin columna no hay forma de distinguirlas, y contándolas un precargado con 2 cargas y cupo 2 **nunca** podía traer acompañante — el fallo que la fase arregla. Medido: 2 cargas + acompañante → 2 filas antes, 3 después | **Restar las cargas del cupo en vez de excluirlas** (cupo efectivo = máximo − cargas). Descartada porque es la misma cuenta con otro nombre: sigue dejando sin acompañante a quien tiene cargas hasta el tope, que es justo el caso roto | **Media.** La columna es aditiva y no destructiva (`ADD COLUMN IF NOT EXISTS`, defecto `MANUAL`), pero revertir exigiría decidir qué hacer con las filas ya marcadas `PUBLIC_FORM` |
| **D1.3** | 01 · Fase 1 | **Cuando llegan más invitados que cupo, se aceptan los que caben y la respuesta lo dice**: el 201 lleva `guestsCreated`, `guestsSkipped` y `guestCap`, y las dos landings lo muestran en pantalla | Rechazar la inscripción entera castiga al asistente por un límite que la interfaz no le enseñó. Descartar en silencio es lo que había, y es lo que produjo el **correo que listaba acompañantes que nunca se guardaron** | **Devolver 400 y rechazar la inscripción completa.** Descartada porque el participante pierde su inscripción por un invitado sobrante, y porque el cliente ya no ofrece más casillas de las que caben: llegar al tope requiere saltarse la interfaz | **Baja.** Son tres campos añadidos a una respuesta; ningún cliente existente los exigía |
| **D2.1** | 01 · Fase 2 | **Un participante ya inscrito no recibe NINGÚN invitado nuevo de la petición**, además de no ver modificados los suyos. La invariante queda en una línea: si `existingScheduleIds.length > 0`, solo se enlaza la fecha nueva | El plan nombraba dos escrituras (el recorte de `guestCount`/`guestLoads` y la rama `g.id`); la creación de invitados nuevos era una tercera que no nombraba. Crear filas en la ficha de otra persona es escribir en sus datos aunque no sobrescriba ninguna columna, y el disparador es el mismo: `lookup` entrega `participantId` y los `id` de las cargas **sin autenticación** a quien acierte un RUT | **Dejar viva la creación de invitados en fechas adicionales**, por si alguien se inscribe a una segunda fecha con acompañante. Descartada porque abre por defecto un camino de escritura no autenticada para salvar un caso que el plan no reclama, y porque contradice la decisión que el propio código ya tomaba: quien ya está inscrito no se modifica solo, contacta con una persona | **Baja.** Es mover la llamada fuera del `else`. Si el caso multi-fecha con acompañante resulta necesario, se reabre con su propia verificación |
| **D3.1** | 01 · Fase 3 | **Los topes se derivan de la columna (255) y la columna NO se amplía.** `dietary_comments` sigue siendo `VARCHAR(255)` | D1.5 admitía ampliar a TEXT si la columna se quedaba corta, pero la propia verificación exigida por la fase 3 dice «comentario de 300 caracteres → 400 con mensaje claro»: ampliar la columna haría que 300 se guardara y **contradiría el criterio de aceptación del plan**. 255 caracteres son ~3 frases, suficientes para describir una alergia, y el freno ahora se ve en el formulario (`maxLength`) en vez de descubrirse al enviar | **Ampliar `dietary_comments` a TEXT** con una migración aditiva como la de la fase 1. Descartada por lo anterior y porque agranda el cambio sin que nadie haya pedido más espacio: la deuda queda anotada y es barata de revertir si aparece un caso real | **Baja.** Ampliar después es `ALTER TABLE ... TYPE TEXT`, aditivo y sin pérdida; solo habría que subir el `.max()` a la vez |
| **D4.1** | 01 · Fase 4 | **`validation.error.format()` se sigue devolviendo al público.** Se deja para **F2-05 (plan 04)** y queda anotado en el código | El plan ofrecía explícitamente las dos salidas. Hoy ese detalle es **lo único que dice al asistente QUÉ campo rechazó la petición**, y la fase 3 acaba de apoyarse en ello: un comentario de dieta demasiado largo devuelve el nombre del campo y el límite. Quitarlo ahora, sin poner antes un mensaje por campo, convertiría un 400 explicable en uno mudo — cambiaría un fallo de seguridad menor por uno de usabilidad seguro | **Recortar ya a un error genérico.** Descartada por lo anterior. La corrección buena no es borrar el detalle sino **traducir los `issues` de Zod a mensajes de usuario**, y eso es trabajo del plan 04, que revisa las fugas de error de toda la API | **Baja**, y aumenta si se retrasa: cuantos más clientes lean `details`, más caro es cambiar su forma |
| **D1.4** | 01 · Fase 1 | **Las cargas precargadas se reenvían con su `id`** y se muestran en modo lectura en `PublicRegistrationForm` | Se reenviaban **sin `id`**, así que el servidor las recreaba: un duplicado por inscripción. El tope de invitados lo tapaba, no lo corregía — en cuanto D1.2 dejó de contarlas, habría vuelto a duplicar | **Arreglarlo en un plan aparte.** Descartada porque D1.2 y este bug están acoplados: cerrar D1.2 sin esto es reintroducir a sabiendas un fallo conocido | **Baja.** Es el `id` en el mapeo del lookup y en el envío |
| **D2.2** | 02 · Fase 1 | **La clave del cubo de credenciales pasa de `email` a `(email, ip)`**: `account:<email>::<ip>`, con la IP de `clientIdentifier` | Una clave que solo contiene el email es un valor que **elige el atacante**: once peticiones con el email de la víctima la dejaban fuera 15 minutos, renovables para siempre. Con `(email, ip)` quien ataca agota su propia cuota desde su origen y la víctima entra desde el suyo. **Trade-off declarado y aceptado**: un atacante con muchas IPs recupera intentos por IP — necesita una botnet, mientras que el DoS de cuenta lo ejecutaba cualquiera con `curl`. Verificado en ambos sentidos: atacante agota 10 intentos contra `admin@…` → 429; el administrador entra con 200 desde otra IP | **Mantener la clave por email y confiar solo en consumir-en-fallo (D2.3).** Descartada porque el atacante sigue generando fallos a voluntad contra la cuenta de la víctima: agotaría la cuota compartida y el 429 de la comprobación previa dejaría fuera a la víctima desde cualquier IP — el mismo DoS con un paso más | **Baja.** La clave se construye en una sola función (`accountKey`, `auth-rate-limit.ts`); cambiar la composición no toca el esquema ni los datos |
| **P02-D2.4** | 02 · Fase 2 | **El límite general se parte en dos cubos: autenticado (JWT con firma VÁLIDA) por USUARIO, anónimo por IP.** Entrar al cubo holgado exige verificar la firma HS256 en el middleware (librería `jose`, dependencia nueva: el runtime Edge no carga `jsonwebtoken`); una cabecera `Authorization` inventada, un token caducado o sin secreto configurado caen al cubo anónimo | Un límite por IP identifica a una sede entera —N puestos tras un NAT— como si fuera un cliente: era el fallo de fondo, no solo el número. Por usuario, el número de puestos deja de importar. Verificar la firma es imprescindible: clasificar por mera presencia de la cabecera dejaría al atacante elegir su cubo con un `Bearer` inventado. El cubo anónimo se queda en 120/min **sin relajar nada** respecto a la remediación: relajar tras un compromiso necesita argumento y el anónimo no lo tiene | **Subir el umbral único por IP a 600/min sin separar** (el plan lo admitía como plan B registrando la deuda). Descartada porque mantiene el modelo equivocado (sede = cliente), quintuplica lo que puede hacer un barrido anónimo, y separar resultó barato (~40 líneas + `jose`, sin infraestructura). También se descartó verificar a mano con Web Crypto para evitar la dependencia: firma hecha a mano en plena remediación es más riesgo que `jose` (auditada, cero dependencias, Edge y Node) | **Baja.** Los dos cubos y la clasificación viven en `rate-limit.ts`; volver al cubo único es borrar la función de clasificación. Quitar `jose` solo exige otra forma de verificar en Edge |
| **P02-D2.1** | 02 · Fase 2 | **Umbrales: autenticado 600/min POR USUARIO; anónimo 120/min por IP** (el valor que ya estaba). Medido, no estimado: el flujo real de acreditación son `S + 7 + 2N` peticiones por persona (S=búsquedas 1-3, N=invitados con nombre; típico 10-14), verificado ejecutando el flujo completo contra la instancia: 4 puestos × 3 personas/min ≈ **170 req/min reales** desde una IP | 600/min por usuario cubre el caso degenerado de una sede ENTERA con una sola cuenta compartida (6 puestos × ~42 req/min ≈ 252/min) con más del doble de margen, y sigue frenando un script con token robado (10 req/s; el «límite» original que no limitaba era 166 req/s). El 120/min anónimo demostró en la verificación que corta un barrido en la petición ~115-121 y que el tráfico anónimo legítimo (login + refresh de 4 puestos) ni se acerca | **600/min por IP como decía la tabla del plan.** Descartada porque la medición y D2.4 la desmienten a medias: el número 600 sobrevive, pero colgado de la IP volvería a castigar a la sede grande y obligaría a subir también lo que un anónimo puede disparar. También se descartó un umbral autenticado por (usuario, IP): no aporta nada contra un token robado usado desde una botnet y sí parte la cuota de un operador que cambie de red | **Baja.** Dos constantes (`AUTH_POINTS`, `ANON_POINTS`) con el razonamiento al lado; recalibrar es cambiar un número y re-ejecutar la batería W3 |
| **P02-D3.1** | 02 · Fase 3a | **La clave del cubo de credenciales se acota con SHA-256 solo cuando no cabe** en los 247 chars útiles de `rate_limits.key` (255 menos `account:`), y **ambos `loginSchema` ganan `.max(254)`** (RFC 5321) en el email — hay dos, uno del servidor y otro del formulario (SB-31) | Una clave que no cabía hacía el limitador **fail-open**: el SELECT de `get()` no falla con un parámetro largo («sin cuota gastada»), el INSERT de `consume()` moría con 22001, el insuranceLimiter lo absorbía y el 429 no llegaba jamás, dejando además cada clave retenida en MemoryStorage sin tope. El hash hex (64 chars) cabe siempre y no colisiona con claves en claro (toda clave en claro contiene `@` y `::`, que no son hex). Verificado: email de 250 chars → 10×401 + 429, con la fila hasheada en PostgreSQL | **Truncar la clave**: permite fabricar colisiones eligiendo prefijos iguales (emails largos con el mismo arranque compartirían cubo a voluntad del atacante). **Hashear siempre**: perdería la legibilidad de `rate_limits` para diagnóstico (hoy se lee `account:email::ip` a simple vista) sin ganar nada — la rama del hash solo se alcanza con claves anómalas | **Baja.** Todo vive en `accountKey()`; cambiar la estrategia no toca esquema ni datos (las claves expiran solas en 15 min) |
| **P02-D3.2** | 02 · Fase 3b | **La conexión del limitador a PostgreSQL se reintenta cada 30 s hasta conseguirse**, con sonda `SELECT` y construcción con `tableCreated: true`; la tabla la garantiza el aprovisionamiento (`db:sync` ahora la crea; en producción, plan 08/D8.4) y el CREATE en caliente queda solo como cortesía para desarrollo (código 42P01). La recuperación se registra igual que la degradación, y el seguro de memoria es único por prefijo para que los contadores sobrevivan a los reintentos fallidos | La degradación era **permanente y silenciosa**: un arranque con la base caída —o sin permiso `CREATE`, lo normal en la administrada de DigitalOcean con PostgreSQL 15+— dejaba el contador en memoria para toda la vida del proceso. La sonda NO puede ser el `CREATE TABLE IF NOT EXISTS` de la librería: **comprobado contra PostgreSQL 16** que falla con `42501 permission denied` AUNQUE la tabla exista si el usuario no tiene `CREATE` en el esquema — con la tabla aprovisionada, la app jamás saldría de memoria. Verificado: arranque con PG caído → degrada y corta en memoria (429 en la petición 61); PG arriba → «vuelve a PostgreSQL tras 68s» y los umbrales siguen (10×401 + 429 con filas en `rate_limits`) | **Solo `tableCreated: true` sin reintento** (la otra opción que el plan ofrecía): elimina la necesidad de CREATE pero un fallo de conexión en el arranque seguiría degradando para siempre, que es el defecto de fondo. **Reintento con timer (`setInterval`)** en vez de al paso de las peticiones: un timer por proceso sigue latiendo aunque nadie use el login y complica el ciclo de vida en Next; disparar el reintento desde `getLimiter` solo corre cuando hace falta | **Baja.** `RETRY_MS` y `connectPostgres()` en un solo fichero; los contadores de memoria de una degradación no se migran a PostgreSQL al volver (pérdida asumida y documentada: como mucho regala los puntos no gastados de 15 min) |
| **P02-D3.3** | 02 · Fase 3c | **El email se normaliza EN ESCRITURA** (hook `beforeValidate` del modelo `User`: trim + minúsculas via `normalizeEmail()`), el login busca por la forma canónica y los controles de duplicado comparan contra ella. **Sin backfill**: no hay datos legados que reescribir | Es la opción que el plan recomienda y la única que deja los DATOS consistentes, no solo las consultas. Antes la grafía del alta era la única llave de entrada (`ADMIN@Example.COM` con contraseña correcta → 401) y el control de duplicados no veía variantes de mayúsculas (`ADMIN@example.com` se creaba como segundo usuario). El backfill no hace falta: los seeds están en minúsculas y la base de producción anterior murió con el droplet (D0) — no existen filas con mayúsculas | **Comparar con `LOWER(email)` en cada consulta** (normalizar en lectura): reparte la regla por todas las consultas presentes y futuras, renuncia al índice único como garantía contra duplicados por grafía y deja los datos sucios para siempre. **Backfill inmediato** (`UPDATE users SET email = lower(trim(email))`): descartado por innecesario hoy; queda PROPUESTO como prerequisito si alguna vez se restaura un volcado antiguo, porque puede chocar con la unicidad y eso exige decisión humana | **Baja-media.** El hook y el helper son un punto único; el coste real aparecería solo al importar datos legados con mayúsculas, que hoy no existen |
| **D2.3 — CORREGIDA POR EL CRÍTICO** | 02 · Fase 1 | **La cuota de credenciales solo se consume cuando el intento FALLA.** Se comprueba con `get()` (sin consumir) antes de verificar la contraseña y se consume en el camino de fallo (credenciales inválidas o cuenta deshabilitada); un login correcto borra la clave `(email, ip)` | Consumir antes de verificar era la mitad del DoS: con la cuota agotada el 429 llegaba **antes** que `authService.login`, así que el reset del acierto no corría nunca. Consumiendo solo en fallo, quien acierta entra **mientras quede al menos un punto** (cuota parcialmente gastada, 1-9 fallos; verificado: 3 fallos + acierto a la 4ª → 200 y la fila del limitador desaparece) y el tráfico legítimo no gasta nada. **Corrección del crítico:** la redacción original decía «entra aunque la cuota esté gastada», y es falso en el caso límite — con la cuota AGOTADA (10/10) el acierto también recibe 429 hasta que expira la ventana de 900 s. Es el comportamiento CORRECTO (dejarlo pasar permitiría adivinar indefinidamente a coste de bcrypt, y la verificación del plan exige que el atacante quede cortado), pero hay que decirlo tal cual: un usuario legítimo que falle 10 veces en 15 min desde su IP queda fuera hasta que expira la ventana. Comentarios del código rectificados en `603a080` | **Consumir siempre y devolver el punto en el éxito** (lo que había). Descartada porque reproduce el fallo: agotada la cuota, la devolución es inalcanzable. También se descartó no comprobar antes de bcrypt: dejaría gastar CPU de hash con la cuota ya agotada, que es la otra mitad de lo que el plan pide evitar | **Baja.** Son dos funciones (`checkAccountRateLimit`, `penalizeAccountRateLimit`) y el orden de llamadas en `login/route.ts`; no hay datos persistentes que migrar |
| **P08-D8.1** | 08 · Fase 1 | **El correo de contacto (y las dos claves EmailJS) se quedan como valores DE BUILD, documentados**: `.example.env` avisa de que las tres `NEXT_PUBLIC_*` se inlinean en `next build` y que cambiarlas exige recompilar y redesplegar | Es el valor por defecto del plan y el código lo confirma: verificado sobre el build que el nombre de la variable tiene CERO apariciones en el JS ejecutable de `.next/server` (solo sobrevive en sourcemaps) y que el literal (`confirmaciones@grupolocastillo.com`, el respaldo de `contact.ts:8`, porque el `.env` local la trae vacía) queda horneado en 1 chunk de cliente y 3 de servidor. Configurable en caliente sería trabajo real por un valor que cambia una vez al año | **Servirlo desde el servidor** (endpoint o page prop) para hacerlo configurable sin recompilar. Descartada por coste/beneficio: obliga a tocar cinco componentes cliente y una ruta, y el valor cambia ~1 vez/año. La vía queda escrita en la plantilla por si algún día compensa | **Media-baja.** Migrar a servirlo del servidor es ~1-2 h y no toca datos; mientras tanto el coste operativo es «recompilar para cambiar un correo» |
| **P08-D8.2** | 08 · Fase 1 | **El middleware (`src/middleware/security.ts`) es la fuente ÚNICA de CORS: las cuatro cabeceras `Access-Control-*` se retiran de `next.config.js`** (las estáticas —CSP, HSTS, etc.— se quedan: no leen `process.env`) | `next.config.js` se evalúa en `next build` y se serializa en `routes-manifest.json`: `ALLOWED_ORIGIN` quedaba CONGELADA con el valor del build (comprobado: el manifiesto traía el valor del `.env` de desarrollo — que además, con comas, ni siquiera es un ACAO válido — y el build del plan traía `*` junto a `Allow-Credentials: true`, F2-04). El middleware lee la variable EN EJECUCIÓN. Verificado a nivel HTTP: página pública sin ninguna cabecera CORS, `/api/health` con origen permitido → UNA sola `access-control-allow-origin` con el origen real, origen no permitido → ninguna | **Cargar el entorno también en `npm run build`** para que el config congelara el valor correcto. Descartada porque deja DOS fuentes que pueden divergir (SB-02), ata el origen al momento del build (cambiar de dominio exigiría recompilar) y mantiene el modo de fallo silencioso si alguien construye sin el entorno | **Baja.** Reponer cabeceras en el config es reescribir el bloque borrado; el riesgo real es el inverso y quedó anotado como SB-33 (la convención `middleware` está deprecada en Next 16) |
| **P08-D8.3** | 08 · Fase 1 | **`NODE_ENV="production"` en `.example.env`**, con el mecanismo real documentado en la plantilla, en `env.ts` y rectificado en el plan 08 | La plantilla es del servidor y el desarrollo local ya la sobrescribe. **El código desmintió el mecanismo del plan** (gana el código): Turbopack INLINEA `process.env.NODE_ENV` en `next build`, así que un `development` heredado NO relaja la validación del artefacto compilado (comprobado: `next start` con `NODE_ENV=development` siguió abortando por las exigencias «en producción», y los chunks tienen `logging:!1` y cero lecturas de `NODE_ENV`). El peligro real es doble: `next build` con `development` heredado —el camino natural `set -a; . /etc/tuacreditacion.env`— HOY FALLA con error de prerender (y si no fallara produciría un build de desarrollo servido como producción), y los scripts tsx (db:sync, seeds) sí leen el `NODE_ENV` real | **Dejar `development` con la advertencia en comentario** (lo que había): demostrado que no funciona — es la trampa que el propio plan documenta. También se descartó quitar la variable de la plantilla: dejarla sin declarar invita a heredar la del shell, que es peor | **Trivial.** Es un valor en una plantilla; quien desarrolle en local usa su propio `.env` con `development` |
| **P08-D8.4** | 08 · Fase 2 | **La tabla `rate_limits` la crea el APROVISIONAMIENTO y la aplicación solo necesita DML** (`SELECT, INSERT, UPDATE, DELETE` — DELETE incluido porque `clearExpiredByTimeout` viene activo); la degradación a memoria del limitador es RUIDOSA, con reintento cada 30 s | El grueso ya estaba implementado por **P02-D3.2** (sonda `SELECT`, `tableCreated: true`, reintento, log en cada fallo) y `db:sync` ya creaba la tabla: esta fase lo **verificó de punta a punta** contra PostgreSQL 16 con un rol sin `CREATE` (`has_schema_privilege = f`). Con la tabla aprovisionada y solo DML, el limitador escribe en PostgreSQL (filas `auth:...` y `account:...` visibles tras un 401) sin una sola línea de degradación; SIN la tabla, el primer login emite `console.error` nombrando el limitador («degrada a memoria (fail-closed) y se reintenta en 30s», `permission denied for schema public`) y lo repite en cada reintento — no es silencioso. Qué permisos trae el usuario de la administrada de DigitalOcean lo responde el plan 07, fase 2 (I4) | **Conceder `CREATE` al usuario de aplicación** para que la librería cree la tabla en caliente. Descartada porque amplía permisos permanentes para una necesidad que desaparece aprovisionando una tabla de tres columnas, y deja viva la ventana de degradación que este plan cierra | **Baja.** El DDL vive duplicado a propósito en `src/scripts/sync-db.ts` y `auth-rate-limit.ts`; conceder o revocar DML es un `GRANT` en la consola de DigitalOcean |
| **P08-D8.6** | 08 · Fase 2 | **`PORT` declarada en la plantilla (`3000`) y en el esquema de `env.ts` (numérica, 1-65535, OPCIONAL); `npm start` fuerza `-H 127.0.0.1` explícito** (D7) | Era la única variable del inventario sin declarar ni validar, y decide a qué upstream apunta Nginx. El bind a loopback es requisito del rate-limit por IP (`clientIdentifier` confía en el último `x-forwarded-for` solo si Nginx es el único camino de entrada). Medido: `Get-NetTCPConnection` → `127.0.0.1:3100 Listen`, no `0.0.0.0`. **El código desmintió a medias la «validación»**: con `next start`, un PORT malformado nunca llega a `validateEnv` — el CLI de Next rechaza `abc` nombrando la variable, y `70000` revienta el bind (`ERR_SOCKET_BAD_PORT`) ANTES de `register()`. La entrada del esquema completa el inventario y cubre arranques futuros que no pasen por el CLI; así queda anotado en el propio esquema | **Hacerla obligatoria en producción.** Descartada porque `-p` por CLI y el defecto 3000 son caminos legítimos (la batería W3 documentada usa `-p 3100`), y un PORT ausente falla ruidoso en Nginx (502), no silencioso | **Trivial.** Un valor en la plantilla y una entrada opcional del esquema |
| **P08-D8.7** | 08 · Fase 2 | **`sequelize.authenticate()` en el arranque (`instrumentation.register`), SOLO en producción: una base inalcanzable es un ARRANQUE FALLIDO**, no un 500 en la primera petición | El plan pedía «valorar» esto; se decide que sí. Sin él, el proceso arrancaba «sano» con la base caída, systemd lo daba por activo y el fallo salía delante del primer usuario. Con la administrada de DigitalOcean y la CA pendiente (SB-09), el primer arranque VA a fallar — y debe fallar en el arranque, donde se mira. Medido: `DATABASE_URL` a un puerto muerto → exit 1 con `ECONNREFUSED` nombrando la variable (jamás su valor: lleva credenciales) y cero procesos escuchando | **Hacerlo también en desarrollo**: descartada porque `next dev` debe poder arrancar sin base (trabajo de UI puro) y las exigencias relajadas en dev son la convención declarada de `env.ts`. **Un healthcheck en vez de abortar**: descartado porque systemd da por activo lo que no muere; abortar es lo que `Restart=` sabe manejar | **Baja.** Un try/catch en `register()`; quitarlo es borrar el bloque |
| **P08-D8.5** | 08 · Fase 3 | **`npm run db:sync` hace `sync({ alter: true })` por defecto; el DROP total exige `FORCE_SYNC=yes` EXACTO** (otro valor de `FORCE_SYNC`, o `--force` sin la variable, ABORTAN con exit 1 sin tocar nada); **el huérfano `scripts/sync-db.ts` se elimina** y el camino de producción pasan a ser las migraciones (`db:migrate`) | Era el valor por defecto del plan y el código lo confirmó: la peligrosa (`force`) tenía atajo npm y la segura (`alter`) vivía en un fichero sin script (SB-32). Un valor inválido de `FORCE_SYNC` aborta EN VEZ de degradar en silencio a `alter`: quien escribió `FORCE_SYNC=1` quería destruir, y hacer otra cosa sin avisar es el mismo modo de fallo silencioso que esta remediación combate. Verificado en ambos sentidos contra la base con datos: `db:sync` → 4 usuarios antes y después; `FORCE_SYNC=1` y `--force` → exit 1 y datos intactos; `FORCE_SYNC=yes` → aviso «PÉRDIDA TOTAL» y base recreada | **Dar a la destructiva su propio atajo (`db:sync:force`)**: descartada porque reintroduce exactamente el defecto (la peligrosa con atajo cómodo); la confirmación debe ser deliberada, no autocompletable. **Apuntar `db:sync` al huérfano `scripts/sync-db.ts`**: descartada porque dejaba vivos los dos ficheros divergentes (el hazard de SB-32) y perdía el `CREATE TABLE rate_limits` de P08-D8.4 | **Baja.** Todo vive en `src/scripts/sync-db.ts`; cambiar la política de confirmación es tocar dos condiciones al principio del fichero |
| **P08-D4.1** | 08 · Fase 3 | **La línea base de migraciones (`migrations/0001-baseline.ts`) se apoya en `sequelize.sync()` sin opciones** (CREATE TABLE IF NOT EXISTS de los 16 modelos) **más el DDL de `rate_limits`**, con umzug 3 + SequelizeStorage (`sequelize_meta`) como runner (`npm run db:migrate` / `db:migrate:status`); su `down()` se niega a revertir. Todo cambio de esquema posterior va en migración nueva con DDL explícito | Escribir a mano el DDL de 16 tablas con sus enums, índices y FKs es ~2 días de trabajo propenso a divergir de los modelos en el primer error, para un beneficio que hoy no existe (no hay bases legadas que reproducir esquemas antiguos: D0 — la base anterior murió con el droplet). `sync()` create-only es idempotente y NO destructivo, verificado en los dos escenarios que importan: base poblada → 4 usuarios antes y después, solo apunta la fila en `sequelize_meta`; base VACÍA (`DROP SCHEMA CASCADE`) → 18 tablas creadas y los seeds corren encima. El trade-off (la baseline describe los modelos del commit en que corre, no un esquema congelado) queda escrito en el propio fichero | **DDL a mano en la baseline** (el esquema congelado ortodoxo): descartada por coste/beneficio hoy — se vuelve necesaria solo si alguna vez hay que reconstruir un esquema histórico, y entonces se genera desde `pg_dump --schema-only` de una base real. **`sequelize-cli`**: descartada porque exige config propia (`.sequelizerc`, CJS) y otro formato de fichero; umzug es la librería que la propia CLI usa por debajo, mantenida por el equipo de Sequelize, y se integra con la instancia y los modelos ya existentes | **Media-baja.** Sustituir la baseline por DDL explícito es una migración nueva más el borrado de la 0001 en bases aún no migradas; el formato de fichero (up/down exportados) no cambiaría |
| **P02-DOC** | 02 · Fase 2 | **Esquema de numeración `P<plan>-D<n.m>` para las entradas nuevas de este registro** (la regla del encabezado, fijada 2026-08-06); las filas existentes conservan su ID, desambiguadas por la columna *Plan · Fase* | La colisión era real: la tabla ya tenía un `D2.1` del plan 01 y la tabla del plan 02 empieza en `D2.1` — sin prefijo habría dos IDs iguales con significados distintos. El prefijo mantiene la trazabilidad 1:1 con la tabla de cada plan | **Numeración secuencial propia del registro, renumerando lo existente.** Descartada porque rompe la correspondencia con las tablas de los planes y renumerar filas ya citadas en commits invalida las referencias | **Trivial.** Convención documental anotada en el encabezado del propio registro |

---

## Esperan tu respuesta

Propuestas **destructivas o difíciles de revertir** que, según el criterio de este registro, no se
deciden solas. Ninguna bloquea nada hoy; todas quedan documentadas como prerequisito.

1. **Reescribir la historia de git para eliminar `acreditacion_dump.sql` (SB-25 / P08-D12).**
   La fase 3 del plan 08 ya hizo lo que no requería permiso: el volcado (6 usuarios con hashes,
   110 refresh tokens, 360 registros de auditoría, 3 empleados con RUT) **ya no está rastreado**
   y `.gitignore` veta `*.sql`/`*.dump` — un clon nuevo no lo recibe en el árbol de trabajo. Pero
   **sigue completo en la historia** del repositorio público (`git show <commit>:acreditacion_dump.sql`
   lo recupera). Reescribir (`git filter-repo --invert-paths --path acreditacion_dump.sql`) lo
   eliminaría de todos los commits, **invalida todos los clones existentes** (todo colaborador
   re-clona; los PR/ramas abiertos divergen) y **no borra lo que ya se haya copiado** del repo
   público. *Qué se te pide:* decidir si se reescribe (y cuándo — el mejor momento es ANTES de
   que la reconstrucción multiplique los clones) o si se deja la historia como está a sabiendas.

2. **Rotar las contraseñas de los 6 usuarios del volcado (SB-25).** Sus hashes bcrypt (coste 12)
   están publicados: no es crackeo inmediato, pero es exposición permanente y afecta a personas.
   *Qué se te pide:* autorizar la rotación (y avisar a los afectados) o aceptar el riesgo
   explícitamente. Independiente de la decisión 1: rotar cierra el riesgo aunque la historia no
   se reescriba.

3. **Backfill de emails legados** — `UPDATE users SET email = lower(trim(email))` — **SOLO si
   alguna vez se restaura un volcado anterior a R2-03c** (plan 02, fase 3c, `13a5aee`). Puede
   chocar con el índice único si en ese volcado existieran dos cuentas que difieren solo en
   mayúsculas, y **decidir qué cuenta sobrevive es una decisión humana sobre datos de usuarios**.
   Hoy no hace falta: no hay filas legadas (D0 — la base anterior murió con el droplet y los
   seeds están en minúsculas). *Qué se te pide:* validar que cualquier restauración de un volcado
   pre-R2-03c lleve este backfill como paso previo obligatorio, y decidir en su momento la regla
   de supervivencia si aparece una colisión.

---

## Valores por defecto ya razonados

Estos no salen de la nada: son el punto de partida que cada plan lleva escrito, para que el
implementador no tenga que inventarlos y el crítico tenga contra qué contrastar. **Si el código
desmiente el razonamiento, gana el código** y la decisión se anota rectificada.

| Asunto | Valor por defecto | Razonamiento |
|---|---|---|
| Cupo de invitados cuando el evento no declara máximo | **2** (el `defaultValue` del modelo) | `Event.ts:99-102` ya lo declara; el `.default(0)` del esquema es lo que lo pisa. Elegir 2 devuelve el comportamiento que el producto tenía antes, que es «suficientemente funcional» |
| Cargas precargadas frente al cupo del asistente | **No lo consumen** | Son presupuesto del organizador, no del asistente. Con la lectura contraria, un precargado con cargas nunca puede traer acompañante — que es el fallo actual |
| Límite general de la API por IP | **~600/min**, revisable | Seis puestos × 3 personas/min × 14 peticiones ≈ 250/min. El doble deja margen para picos sin volver al 166 req/s original |
| Cota de reintento tras un 401 | **1** | Si el segundo intento también falla, el problema no es el token |
| `SameSite` de las cookies | **`Lax`** | `Strict` rompe la vuelta desde un enlace de correo, y el producto manda correos de confirmación |
| Base de datos en los tests | **Contenedor efímero** | Los fallos que esta remediación introdujo son de comportamiento contra la base; un mock no los habría visto |
| Umbral de cobertura | **El que haya al ponerlos en verde**, subiendo después | Un 80 % de golpe genera test de relleno. Mejor un número real que suba que uno alto que se ignora |
| Correo de contacto configurable | **De build, documentado** | Hacerlo dinámico obliga a servirlo desde el servidor; el valor cambia una vez al año |
| Gestor de procesos | **systemd, un proceso** | PM2 en cluster multiplica los contadores del limitador y no lee `EnvironmentFile` |
| Historia de git y el volcado | **Solo destrackear**, sin reescribir | Reescribir invalida los clones y no borra lo ya publicado. **Requiere confirmación de Emmanuel** |
